package cc.alessandro.jpocket.filters;

public enum DetailType {

	/**
	 * Return only the titles and urls of each item.
	 */
	SIMPLE,

	/**
	 * Return all data about each item, including tags, images, authors, videos and
	 * more.
	 */
	COMPLETE;
}
